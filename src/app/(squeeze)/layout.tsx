/**
 * Minimal layout for squeeze/lead-magnet pages.
 * No navigation, no footer — one page, one goal.
 */
export default function SqueezeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
